'use client';

import React,{useEffect,useMemo,useState}from'react';
import{createClient}from'@/lib/supabase/client';
import{Button}from'@/components/ui/Button';
import{formatCents}from'@/lib/format';
import{CircleDollarSign,LockKeyhole,ReceiptText,ShieldCheck}from'lucide-react';

type Lot={id:string;code:string;beer_style:string;status:string};
type Entry={lot_id:string;entry_type:string;amount_cents:number;description:string|null};
type Allocation={lot_id:string;participant_user_id:string|null;is_ctg_internal:boolean;case_equivalent_units:number;capital_committed_cents:number};

export default function SettlementPage(){const supabase=useMemo(()=>createClient(),[]);const[lots,setLots]=useState<Lot[]>([]);const[lotId,setLotId]=useState('');const[entries,setEntries]=useState<Entry[]>([]);const[allocations,setAllocations]=useState<Allocation[]>([]);const[confirm,setConfirm]=useState('');const[busy,setBusy]=useState(false);const[message,setMessage]=useState<string|null>(null);const[error,setError]=useState<string|null>(null);
  const load=async()=>{setError(null);const[{data:l,error:le},{data:e,error:ee},{data:a,error:ae}]=await Promise.all([supabase.from('investment_production_lots').select('id,code,beer_style,status').eq('status','SETTLEMENT_PENDING').order('created_at',{ascending:false}),supabase.from('investment_lot_financial_entries').select('lot_id,entry_type,amount_cents,description'),supabase.from('investment_funding_allocations').select('lot_id,participant_user_id,is_ctg_internal,case_equivalent_units,capital_committed_cents')]);if(le||ee||ae){setError(le?.message||ee?.message||ae?.message||'No se pudo cargar conciliación');return;}const rows=(l??[])as Lot[];setLots(rows);setLotId(p=>p||rows[0]?.id||'');setEntries((e??[])as Entry[]);setAllocations((a??[])as Allocation[]);};
  useEffect(()=>{void load();},[]);
  const lot=lots.find(l=>l.id===lotId);const f=entries.filter(e=>e.lot_id===lotId);const alloc=allocations.filter(a=>a.lot_id===lotId);const sum=(type:string)=>f.filter(e=>e.entry_type===type).reduce((s,e)=>s+Number(e.amount_cents||0),0);const revenue=sum('REVENUE'),tax=sum('TAX'),prod=sum('PRODUCTION_COST'),commercial=sum('COMMERCIAL_COST'),adjustment=sum('ADJUSTMENT'),ndlp=revenue-tax-prod-commercial-adjustment;const participantCases=alloc.filter(a=>!a.is_ctg_internal).reduce((s,a)=>s+a.case_equivalent_units,0);const internalCases=alloc.filter(a=>a.is_ctg_internal).reduce((s,a)=>s+a.case_equivalent_units,0);
  const finalize=async()=>{if(!lot||confirm.trim().toUpperCase()!==lot.code.toUpperCase())return;setBusy(true);setError(null);setMessage(null);const{error}=await supabase.rpc('finalize_settlement',{p_lot_id:lot.id});if(error)setError(error.message);else{setMessage(`Settlement finalizado para ${lot.code}.`);setConfirm('');await load();}setBusy(false);};
  return <div className="space-y-6"><header className="rounded-[28px] border border-white/10 p-6 sm:p-8" style={{background:'linear-gradient(135deg,rgba(18,18,18,.98),rgba(7,7,7,.95))'}}><p className="text-[9px] uppercase tracking-[.28em] text-accent mb-3">CTG One · Financial Control</p><h1 className="text-3xl sm:text-5xl font-outfit font-semibold">Settlement Reconciliation</h1><p className="text-sm text-text-muted mt-3 max-w-3xl">Revisa los hechos financieros antes del cierre irreversible. La distribución exacta permanece delegada al motor de settlement de PostgreSQL.</p></header>{(message||error)&&<div className="rounded-xl border px-4 py-3 text-sm" style={{borderColor:error?'rgba(239,68,68,.3)':'rgba(34,197,94,.25)',background:error?'rgba(239,68,68,.06)':'rgba(34,197,94,.05)',color:error?'#fca5a5':'#86efac'}}>{error??message}</div>}<section className="rounded-2xl border border-white/10 p-5 sm:p-6 bg-white/[.02]"><label className="block text-[9px] uppercase tracking-[.13em] text-text-dim mb-2">Lote pendiente de liquidación</label><select className="w-full rounded-xl px-3 py-3 bg-white/[.025] border border-white/10 text-white" value={lotId} onChange={e=>{setLotId(e.target.value);setConfirm('')}}><option value="">Selecciona un lote</option>{lots.map(l=><option key={l.id} value={l.id}>{l.code} · {l.beer_style}</option>)}</select></section>{lot&&<><section className="grid grid-cols-2 xl:grid-cols-5 gap-3"><Metric label="Ingresos" value={formatCents(revenue)} icon={<CircleDollarSign/>}/><Metric label="Impuestos" value={formatCents(tax)} icon={<ReceiptText/>}/><Metric label="Producción" value={formatCents(prod)} icon={<ReceiptText/>}/><Metric label="Comercial" value={formatCents(commercial)} icon={<ReceiptText/>}/><Metric label="NDLP preliminar" value={formatCents(ndlp)} icon={<ShieldCheck/>} accent/></section><section className="grid grid-cols-1 xl:grid-cols-[1.1fr_.9fr] gap-5"><div className="rounded-2xl border border-white/10 p-5 bg-white/[.02]"><p className="text-[9px] uppercase tracking-[.18em] text-text-dim">RECONCILIATION FACTS</p><h2 className="text-xl font-outfit font-semibold mt-1 mb-5">Composición del lote</h2><Row label="Participación externa" value={`${participantCases} cajas equivalentes`}/><Row label="Participación CTG interna" value={`${internalCases} cajas equivalentes`}/><Row label="Allocations registradas" value={String(alloc.length)}/><Row label="Ajustes financieros" value={formatCents(adjustment)}/><p className="text-[11px] text-text-dim mt-5 leading-relaxed">Esta vista no calcula ni reemplaza la distribución final. `finalize_settlement()` usa las allocations y la versión de fórmula fijada para producir el snapshot auditable.</p></div><div className="rounded-2xl border border-accent/20 p-5 bg-accent/[.035]"><div className="flex items-center gap-3 mb-4"><LockKeyhole className="text-accent"/><div><p className="text-[9px] uppercase tracking-[.18em] text-accent">IRREVERSIBLE ACTION</p><h2 className="text-xl font-outfit font-semibold mt-1">Finalizar settlement</h2></div></div><p className="text-xs text-text-muted leading-relaxed mb-5">Confirma que ventas, impuestos, costos y ajustes están conciliados. Para continuar escribe exactamente el código <span className="font-mono text-accent">{lot.code}</span>.</p><input className="w-full rounded-xl px-3 py-3 bg-black/30 border border-white/10 text-white mb-4" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder={lot.code}/><Button onClick={()=>void finalize()} loading={busy} disabled={confirm.trim().toUpperCase()!==lot.code.toUpperCase()} variant="primary" size="sm" fullWidth>Finalizar settlement</Button></div></section></>}</div>}
function Metric({label,value,icon,accent=false}:{label:string;value:string;icon:React.ReactNode;accent?:boolean}){return <div className="rounded-2xl border p-4" style={{borderColor:accent?'rgba(201,169,98,.28)':'rgba(255,255,255,.08)',background:accent?'rgba(201,169,98,.05)':'rgba(255,255,255,.02)'}}><div className="text-accent mb-3">{React.cloneElement(icon as React.ReactElement<{size?:number}>,{size:15})}</div><p className="text-[9px] uppercase tracking-[.12em] text-text-dim">{label}</p><p className="text-base font-outfit font-semibold mt-2">{value}</p></div>}
function Row({label,value}:{label:string;value:string}){return <div className="flex justify-between gap-4 py-3 border-b border-white/[.06]"><span className="text-xs text-text-dim">{label}</span><span className="text-xs text-white text-right">{value}</span></div>}
