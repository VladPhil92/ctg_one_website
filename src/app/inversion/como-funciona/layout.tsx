import type { Metadata } from 'next';

export const metadata: Metadata = {
  alternates: { canonical: 'https://ctgone.com/inversion/como-funciona' },
};

export default function InvestmentHowItWorksLayout({ children }: { children: React.ReactNode }) {
  return children;
}
