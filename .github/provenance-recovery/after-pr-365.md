# Main provenance recovery — PR #365

PR #365 deployed the bounded Privy provider-readiness certification phase but was squash-merged. The Investment BR merged-main provenance contract intentionally accepts only verified two-parent merge commits.

This marker exists solely to create a recovery PR that must be merged with GitHub's regular **merge** method. It does not change application logic, database DDL, investment business rules, wallet authority, identity ownership, or deployment configuration.
