name: Legacy Webpack Workflow (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deprecation
        run: echo "This repository is a Next.js application. Automated validation now runs from .github/workflows/ci.yml using npm ci, TypeScript typecheck, and npm run build."
